package hylyht.adapter.servlet.resource

import org.scalatest.FlatSpec
import hylyht.domain._


class UserResourceEndpointTest extends FlatSpec {

    "UserResourceEndpoint" should "create a user with a unique username and password" in {
        fail()
    }
}
